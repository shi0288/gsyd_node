/**
 * Created by shiqm on 16-3-15.
 */
'use strict';


var amqp = require('amqplib/callback_api');

var esut = require("easy_util");
var log = esut.log;

var MqSource = function () {
    var self = this;
};

MqSource.prototype.init = function (cb) {
    var self = this;
    if (self.conn) {
        cb(null);
    } else {
        amqp.connect('amqp://127.0.0.1', {'noDelay': true}, function (err, conn) {
            self.conn = conn;
            if (!err) {
                log.info('-----MQ连线初始化成功');
                process.once('SIGINT', function () {
                    self.conn.close();
                });
            }
            cb(err);
        });
    }
};

MqSource.prototype.bail = function (err, conn) {
    if (err) {
        conn.close();
    }
};


MqSource.prototype.send = function (queueSend, queueBack, content, cb) {
    var self = this;
    self.conn.createChannel(function (err, ch) {
        if (err !== null) {
            cb(err);
            return self.bail(err, self.conn);
        }
        ch.assertQueue(queueBack, {durable: false}, function (err, ok) {
            if (err !== null) {
                cb(err);
                return self.bail(err, self.conn);
            }
            ch.consume(queueBack, function (msg) {
                cb(null, msg.content.toString());
                ch.ack(msg);
                ch.close();
            }, {noAck: false});
            log.info(" servce send:" + content);
            ch.sendToQueue(queueSend, new Buffer(content), {replyTo: queueBack})
        })
    })
};


var mqSource = new MqSource();
module.exports = mqSource;